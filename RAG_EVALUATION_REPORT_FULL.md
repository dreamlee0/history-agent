# history-rag-chat 检索质量综合测评报告（扩展评测集 · 改进前后对比）

> 测评日期：2026-09-02（当前确定性实测）｜ 对象：history-rag-chat（LangChain + Chroma + bge-small-zh-v1.5 + DeepSeek-v4-flash）
> 独立于基础评测（`RAG_EVALUATION_REPORT.md`，26 条受控集）；本报告使用扩展评测集（44 条起步，第三轮扩容至 198 条），
> 在同一向量库 / 同一评测脚本上给出**改进前 → 改进后**多轮实测对比，数据可一键复现（§12）。
> **第三轮（2026-09-02）** 五大问题整改（97/97 资料补全 / 多相关联合检索 / 决策门简化 / 测试扩容 / 在线验证）见 **§14**。

---

## 1. 测评概述

**动机**：基础评测集（26 条）为人工挑选的受控场景，实测接近 100%，不代表真实分布。本报告保留基础样本，补充三类难样本（高相似跨人陷阱 / 多相关主题 / 库外负面）重新实测，并在此基线（§2~§5"改进前"列）基础上实施检索层改进，量化前后差异。

**评测集构成**（`data/eval/retrieval_eval_full.json`，首轮 44 条 → **第三轮 198 条**，train/holdout 分层）：

| 类别 | 条数（44 → 198） | 说明 |
|------|------|------|
| 单相关（same/cross/event/cross_trap） | 26 → 145 | 基础受控集 + 高相似跨人陷阱（诸葛亮问曹操等） |
| 多相关（multi） | 6 → 31 | 主题枚举题，期望人物为一组 |
| 库外负面（negative） | 6 → 22 | 查询对象不在 97 人知识库（钱学森、袁隆平、拿破仑等） |
| 分层切分 | — | train 159 / holdout 39（按 type 80/20，阈值只在 train 标定、holdout 上报泛化） |

**指标口径**：检索距离越小越相关，k=3。单相关以 Hit@1 / Hit@3(=Recall@3) / MRR / 防污染门决策正确率；多相关以 Recall@3（命中期望人物数 / 期望人物数）；另有 RAGAS 风格 Context Recall@3 / Precision@3（单+多相关共 38 条，离线可算）；库外以拒绝率（走 no-RAG 分支）与自传注入率衡量。

**本轮改进摘要**（改进前为基线，改进后为当前代码实测）：

| 改进项 | 实现 | 对应章节 |
|--------|------|----------|
| 混合重排 Rerank | 候选池 k_fetch=15 → jieba+TF-IDF 词法 × 稠密向量 RRF 融合 → top-k（完全离线，`rerank_mode=hybrid`） | §2/§4 |
| 决策门 1 强相关全局门 | 全局强相关（best_g<0.70）且过滤明显更差（Δ>0.10）→ 强制退回全局 | §2.3 |
| 决策门 2 库外实体拒绝 | 无自称 + 未点名库内人物 + 点名库外实体（显式名单 + jieba 人名类词性，排除朝代词）→ 走 no-RAG 分支（§14.3 起改为纯人名判据，删距离条件） | §5/§14.3 |
| 分块策略 + 别名元数据 | 短文档（≤600 字）整篇不切碎；97 人别名（李世民→唐太宗等）写入 chunk 元数据 | §6 |
| 评测增强 | 新增 RAGAS 风格上下文指标、库外拒绝率；重建向量库（幂等） | §3/§5 |

**第二轮（2026-09-01，检索再增强）新增**：召回层混合检索（可选 `retrieval_mode=hybrid`，BM25×稠密 RRF）、检索延迟/成本剖析脚本、端到端评测（Mock LLM 离线 + `--llm live`，第三轮已实跑）、多格式文档解析（10 种格式 + 摄取脚本）。详见 §7~§10，工程/复现口径见 §6/§12。

**第二轮总览（2026-09-01 检索再增强，44 条集 / 99 篇库 / 986 分块 —— 历史快照，非当前）**：

| 指标 | similarity（默认） | hybrid |
|------|--------------------|--------|
| 单相关 hit@1 / hit@3 | 29/32 · 32/32 | 30/32 · 32/32 |
| MRR | 0.948 | 0.969 |
| 防污染门决策正确率 | 32/32 = 100% | 32/32 = 100% |
| 多相关 Recall@3 | 0.178 | 0.211 |
| RAGAS Context Recall@3 / Precision@3 | 0.870 / 0.316 | 0.875 / 0.325 |
| 库外拒绝率 / 自传注入率 | 6/6 · 0/6 | 6/6 · 0/6 |
| 单元测试回归 | 96/96 | 96/96 |
| 端到端路由综合 | 38/38 = 100% | — |

> 表中数字为当前代码实测（确定性可复现，§12）；下文 §2~§5 各"改进前"列仍为首轮历史基线（26 条集 / 99 篇库），"改进后"列已更新为当前实测。
>
> **第三轮总览（198 集 / 97/97 库，详见 §14）**：多相关 Recall@7 = **0.745**（train 0.732 / holdout 0.803，改进前 @3=0.307）；
> 库外拒绝率 **22/22**、自传注入率 0/22（改进前 12/22、3/22）；单相关 hit@1 0.897 / MRR 0.933 / 决策 0.986 零回归；
> 单元测试 110/110；在线 Faithfulness/Relevancy/Citation 见 §14.5（真实 LLM 调用）。

---

## 2. 单相关检索质量（32 条）

### 2.1 总体指标（k=3）

| 指标 | 改进前 | 改进后（当前确定性精确检索实测） |
|------|--------|-----------------------------------|
| Hit@1 | 30/32 = **93.8%** | **29/32 = 90.6%** |
| Hit@3 / Recall@3 | 30/32 = **93.8%** | **32/32 = 100%** |
| MRR | 0.938 | **0.948** |
| 防污染门决策正确率 | 30/32 = **93.8%** | **32/32 = 100%** |

> **hit@1 较 2026-08 首轮略降的说明**：本列数字为 gushiwen 真实史源入库（986 分块双轨库）+ 精确确定性检索修复后（2026-09-01 第二轮时点）的实测。hit@1 29/32 vs 首轮 31/32 的差异来自**语料变难而非检索退化**——真实史源原文里同人/近义文本更多，3 例 top-1 被近义人物抢占（陆游/曹操/陶渊明，见 §2.3/§7 案例分析）；但 hit@3 与决策正确率**双双升到 100%**，库外拒绝 6/6、自传注入 0/6 全零回归（§5）。语义检索层在当前数据规模下仍是全命中保证（hit@3 32/32），top-1 的取舍见 §7。

### 2.2 分类型

| 类型 | n | 改进前 Hit@3 | 改进后 Hit@3（当前） |
|------|---|-------------|---------------------|
| same | 14 | 100% | 100% |
| cross | 10 | 100% | 100% |
| event | 2 | 100% | 100% |
| **cross_trap** | 6 | **66.7%** | **100%** |

受控子集（same/cross/event）两轮均满分，无回归；cross_trap 陷阱题改进前挂 2 条，当前精确检索 + 决策门 1/1b 全修复（6/6 全命中，含此前判为"固有歧义"的辛弃疾→苏轼，见 §2.3）。

### 2.3 失败案例分析（自传陷阱）

| 提问 | 期望 | 改进前 | 改进后（当前） |
|------|------|--------|----------------|
| 诸葛亮 问「曹操的军事谋略与用人之道」 | 曹操 | ✗ 注入诸葛亮自传（ratio=1.192） | **✓ 决策门 1b（named_other）触发 → 退回全局；similarity 下曹操 rank1（top1 仍为诸葛亮传），hybrid 下曹操 top-1** |
| 辛弃疾 问「苏轼的豪放词风特点是什么」 | 苏轼 | ✗ 注入辛弃疾自传（ratio=1.030） | **✓ 已修复：决策门 1b（named_other）退回全局，苏轼 top-1** |

**诸葛亮→曹操（similarity 残余，hybrid 修复）**：决策门 1b 识别查询点名库内他人（曹操）→ 退回全局，避免自传注入。相似度模式下全局 top-1 仍被诸葛亮自己的《三国志·诸葛亮传》抢占（"军事谋略"语义与诸葛亮传高度重叠，曹操 rank1），**命中但未 top-1**；hybrid 经 BM25 精确人名通道把曹操传提至 top-1（§7 案例分析）。该例是"退回全局但全局 top-1 仍同人抢占"的语义固有难点。

**辛弃疾→苏轼已修复**：首轮判定为"固有歧义"（ratio=1.030 与同人差 0.03，辛弃疾即豪放派）。决策门 1b（named_other）入库后，查询点名"苏轼"（非提问人辛弃疾）直接退回全局，**苏轼《宋史·苏轼传》真实史源入库后全局 top-1 为苏轼**，陷阱题清零（§2.2 cross_trap 100%）。

### 2.4 防污染门阈值标定（--sweep，改进后）

| ratio | 纯比值重放决策正确率 | 说明 |
|-------|---------------------|------|
| 1.10 | 90.6% | — |
| 1.15 | 90.6% | — |
| **1.20（旧默认）** | **93.8%** | 完整管线（含决策门）首轮实测 96.9% |
| 1.25 | 90.6% | — |
| 1.50 | 81.2% | — |

阈值 1.20 在早期 26 条集上曾是标定点；决策门在其基础上首轮再修复 1 例（诸葛亮→曹操），完整管线 96.9% 高于纯比值重放 93.8%。

> 注：上表为本表对应的**早期 26 条基础集**标定。gushiwen 真实史源入库后，跨人高
> 相似陷阱加剧（3 例 ratio 恰落在 1.20 之下漏过），阈值经新数据扫描**重标定为
> 1.25**（settings 默认，见 §13.2），并新增决策门 1b（named_other）。当前默认
> `FILTERED_SCORE_RATIO=1.25`，44 条扩展集决策正确率 1.000（纯比值重放口径
> 1.25 下为 32/32，决策门全部命中，见 §2.2/§2.3）。

---

## 3. RAGAS 风格上下文指标（单+多相关，38 条）

"检索上下文"= 决策后 top-k 文档集合；相关性 = 文档人物 ∈ 期望人物。

| 指标 | 改进前 | 改进后（当前 similarity 实测） |
|------|--------|----------------------------------|
| 全体 Context Recall@3 | 0.812 | **0.870** |
| 全体 Context Precision@3 | 0.289 | **0.316** |
| 单相关 Recall@3 / Precision@3 | 0.938 / 0.312 | **1.000 / 0.333** |
| 多相关 Recall@3 / Precision@3 | 0.144 / 0.167 | **0.178 / 0.222** |

提升主要来自单相关（陷阱题修复 + 真实史源入库 + 精确检索，单相关 Recall@3 达 1.000，precision 0.333 = 恰好每查询 1 个相关文档/3）与多相关（重排把赤壁之战中曹操的史料提入 top-3，见 §4）。多相关绝对值仍低，源于路径限制而非排序缺陷（见 §4）；hybrid 模式下全体 Recall@3/Precision@3 为 0.875/0.325（§7）。

---

## 4. 多相关主题题召回（6 条）

| 提问 | 期望人物数 | 改进前 Recall@3 | 改进后 Recall@3 |
|------|-----------|-----------------|-----------------|
| 苏轼 问「唐宋八大家中哪些是宋代人」 | 3 | 0.33 | 0.33 |
| 曹操 问「赤壁之战涉及哪些主要人物」 | 5 | 0.20 | **0.40** |
| 白居易 问「唐代有哪些著名诗人」 | 5 | 0.00 | 0.00 |
| 刘邦 问「汉朝的开国功臣有哪些」 | 3 | 0.00 | 0.00 |
| 秦始皇 问「秦朝有哪些重要历史人物」 | 3 | 0.33 | 0.33 |
| 诸葛亮 问「三国时期的主要人物有哪些」 | 7 | 0.00 | 0.00 |

**平均 Recall@3：0.144 → 0.178（similarity，当前实测一致）；至少命中 1 个期望人物占比：50%。hybrid 模式平均 0.211（赤壁之战 0.40→0.60，见 §7）。**

改进后"赤壁之战"由 0.20 提升到 0.40（重排把曹操的史料提入 top-3；hybrid 再经 BM25 人名通道提入孙权/刘备达 0.60）。但整体仍受**设计边界**限制：系统面向"向单个历史人物提问"的沉浸式场景，检索默认按被问人物过滤，候选池只含被问人物自传，top-3 无法覆盖跨人物枚举题的多个期望人物（重排只能改变池内顺序，改变不了池子本身）。主题枚举类提问不在当前检索架构支持范围内，属明确适用范围限制。

---

## 5. 库外负面样本与拒绝能力（6 条）

**改进前**：检索层无拒绝机制，全部 6 条均返回史料——

| 提问（均不在知识库） | top-1 命中 | 路径 |
|---------------------|-----------|------|
| 鲁迅 问「钱学森…」 | 鲁迅（本人自传） | filtered |
| 鲁迅 问「袁隆平…」 | 李鸿章 | global |
| 鲁迅 问「屠呦呦…」 | 鲁迅（本人自传） | filtered |
| 唐太宗 问「钟南山…」 | 孙中山 | global |
| 诸葛亮 问「拿破仑…」 | 诸葛亮（本人自传） | filtered |
| 诸葛亮 问「华盛顿…」 | 诸葛亮（本人自传） | filtered |

**改进前关键数据**：拒绝间隔 = -0.169（库外样本最小距离 0.983 vs 正样本最大距离 1.151，**存在重叠**）→ 检索层仅凭距离分数**无法干净区分库外人物与真实相关人物**；自传注入率 4/6。

**改进后**：决策门 2（库外实体拒绝）——

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| 库外提问拒绝率（走 no-RAG） | 0/6 | **6/6** |
| 自传注入率（top-1 为被问人物自传） | 4/6 | **0/6** |

**机制说明**：距离重叠排除了"距离置信度"方案（钱学森 best_f=1.231/best_g=1.042 与库内事件题隋文帝大运河 best_f=1.221/best_g=1.035 参数几乎相同，纯阈值必然误伤）。最终采用**语义三重合取**：查询无自称（非"问本人"）+ 过滤/全局双侧弱匹配（距离均 > 0.90）+ 点名库外实体（jieba 词性为专名类且不在史料语料词表，如"钱学森/nr""拿破仑"）。库内事件题（"开凿大运河"）不点名库外实体 → 不误伤；库外人物题（钱学森/袁隆平/拿破仑等）三项全中 → 拒绝。该门已并入单元测试（全量 110/110 通过，见 §12）。

---

## 6. 工程与系统指标（实测）

| 指标 | 结果 |
|------|------|
| 单元测试 | **110/110 通过**（pytest，含 97/97 覆盖率断言、BM25 多人物掩码、multi 检测、知识/检索/双轨数据源/多格式文档解析/抓取器/端到端/引用/DB/记忆/防幻觉提示词等，2026-09-02 全量回归零失败） |
| 数据规模 | 97 人物 / 21 朝代 / **206 文件**（99 篇 persona + **107 篇真实史源**：74 gushiwen + 21 百度百科 + 7 A 类重抓 + 5 ctext）/ **1357 向量块**（99 persona + 1258 historical，短文档整篇不切分） |
| 别名元数据 | 29 篇携带人物别称（李太白→李白、孔明→诸葛亮等） |
| 重排 | `rerank_mode=hybrid` 默认开启，完全离线（jieba+sklearn，无额外模型下载） |
| 混合检索 | `retrieval_mode=hybrid` 可选开启（稠密+BM25 RRF 召回，默认 `similarity` 保持既有行为），BM25 索引懒构建缓存 |
| 文档解析 | `src/retrievers/document_loader.py` 支持 txt/md/html/pdf/docx/xml/json/csv/tsv/xlsx 统一解析（前置头/别名/来源元数据一致），`load_knowledge_files` 全格式覆盖 |
| 延迟/成本剖析 | `scripts/benchmark_retrieval.py`（读 `scores["timings"]` 分阶段延迟 + 一次性成本 + LLM 价格估算） |
| 端到端评测 | `scripts/evaluate_end_to_end.py`（Mock LLM 离线跑完整 chat 链路；`--llm live` 已实跑 198 条，见 §14.5 对照一） |
| 多格式摄取 | `scripts/ingest_documents.py`（--src/--mode append|rebuild，append 按文件幂等去重） |
| 生成层评测 | `scripts/evaluate_generation.py`（RAGAS 风格 Faithfulness/Relevancy/Citation，**已实跑 198 条全量**，Faithfulness 0.227 / Relevancy 0.570 / Citation 0.740，见 §14.5） |

---

## 7. 混合检索对比（hybrid vs dense，44 条同集）

**本轮新增召回层混合检索**（`retrieval_mode=hybrid`，可选，默认 `similarity`）：候选池由纯稠密召回改为 **稠密 top(k_fetch) + BM25 词法 top(hybrid_bm25_k) 的 RRF 融合**（K=hybrid_rrf_k，默认 60，与重排器内部融合同口径）。词法通道能把 dense 漏掉的精确人名 / 罕见词 / 枚举片段拉回候选池。

**实现要点（与计划的差异修正）**：

- **路径决策仍基于稠密 top-1 距离**，且由独立 k=1 稠密检索提供，而非"融合池内 min 稠密距离"——融合池按 RRF 截断到 k_fetch 后 dense top-1 可能不在池内（BM25 高排文档挤占），用池内距离会失真（实测曾致库外拒绝 6/6→5/6）。决策与内容分离后：防污染门 / 库外拒绝口径与 similarity 模式**完全一致**。
- BM25 零分文档不返回（只返回 score>0 的命中），避免无关文档以 0 分污染融合排序。

**44 条评测对比（同一评测集、同一 k=3、同一 `rerank_mode=hybrid`，2026-09-01 第二轮时点确定性实测）**：

| 指标 | similarity（默认） | hybrid | 说明 |
|------|--------------------|--------|------|
| 单相关 hit@1 | 29/32 = 90.6% | **30/32 = 93.8%** | 曹操：诸葛亮传抢占 top-1（见下） |
| 单相关 hit@3 | 32/32 = 100% | 32/32 = 100% | **双模式全命中** |
| MRR | 0.948 | **0.969** | 同上（hybrid 修复曹操 top-1） |
| 防污染门决策正确率 | 32/32 = 100% | 32/32 = 100% | 决策与稠密口径一致，零回归 |
| 多相关 Recall@3 | 0.178 | **0.211** | 赤壁之战 0.40→0.60（孙权/刘备被词法召回） |
| Context Recall@3 / Precision@3 | 0.870 / 0.316 | **0.875 / 0.325** | 略升 |
| 库外拒绝率 | 6/6 | **6/6** | 零回归 |
| 自传注入率 | 0/6 | 0/6 | 零回归 |

**案例分析（hybrid 的收益与取舍）**：

- **赤壁之战（多相关 5 人）**：similarity 召回曹操/诸葛亮（2/5=0.40）；hybrid 经 BM25 精确人名通道召回孙权/刘备/诸葛亮（3/5=0.60）——**实打实的提升来源**。
- **诸葛亮→曹操（单相关 hit@1 差）**：similarity 下决策门 1b 已退回全局，但全局 top-1 仍被诸葛亮《三国志·诸葛亮传》抢占（曹操 rank1，命中非 top-1）；hybrid 经 BM25 把曹操传提至 top-1——**hybrid 在 hit@1/MRR 上反超 similarity 的直接来源**（30/32 vs 29/32、0.969 vs 0.948）。
- **苏轼→陆游（两模式共同的 hit@1 失分）**：查询"陆游的爱国诗作"全局 top-1 为李鸿章（清史稿·李鸿章传，洋务爱国语义重叠），陆游 rank1；hybrid 相同。属词法/语义近义人物抢占，top-3 均命中。
- **屈原→陶渊明**：两模式 top-1 均被墨子抢占（墨子非军事文本却与"田园诗"语义距离近），similarity 陶渊明 rank2、hybrid rank1。属语义近邻的固有难点，均在 top-3。

**结论**：hybrid 在多相关枚举召回上有明确收益（+0.033，赤壁之战 0.40→0.60），且在诸葛亮→曹操例上把 hit@1/MRR 反超 similarity（30/32 vs 29/32、0.969 vs 0.948），hit@3 双模式全命中。**默认仍保持 `similarity`**（延迟更低：p50 39.5ms vs hybrid 101.7ms，见 §8；且现有测试/评测口径均以 similarity 为准）；需要多人物枚举 / 精确人名召回 / 更高 MRR 时设 `RETRIEVAL_MODE=hybrid`。

---

## 8. 检索延迟 / 成本剖析（scripts/benchmark_retrieval.py）

对评测集查询跑真实 `_retrieve_knowledge`，读 `scores["timings"]` 分阶段埋点（search_filtered / search_global / bm25 / rerank / total）。实测 20 条（CPU 本地，2026-09-01 第二轮时点，986 分块双轨库 + 精确确定性检索；第三轮扩库至 1357 分块后绝对值略增、量级不变，方法口径一致）：

| 阶段 | similarity p50 | hybrid p50 | 说明 |
|------|----------------|------------|------|
| search_filtered | 7.7ms | 38.2ms | hybrid = 稠密 top(k_fetch) + BM25 + 决策用稠密 k=1 |
| search_global | 7.5ms | 38.1ms | 同上（对称） |
| bm25 | — | ≈0 | 索引构建一次性（见下）后缓存，查询期纯打分 |
| rerank | 23.6ms | 22.6ms | 首次含重排器构建（TF-IDF）一次性成本 |
| **total** | **39.5ms** | **101.7ms** | hybrid 每查询多 2 次稠密检索 + BM25 打分（全库 986 条） |

**一次性成本**：全库 986 篇 embedding（CPU，本地 bge）= ~16.1s，约 20.2 万 token（本地推理，**¥0**）；BM25 索引构建（jieba 词典加载 + 986 文档 45215 词元）= ~3.6s，之后按需缓存复用。

**LLM 成本（离线估算，未真实调用）**：输入按 系统提示 + 检索上下文 + 问题 ≈ p50 1333 token/查询（检索上下文随真实史源 chunk 增大）；输出按平均回复 ~200 字 ≈ 125 token。按默认价格表（`LLM_PRICE_IN=1.0` / `LLM_PRICE_OUT=2.0`，¥/1M token，env 可覆盖）估算**每查询 ≈ ¥0.0015**，20 条合计 ≈ ¥0.031。

**诚实标注**：离线无法实调 LLM 用量与网络往返延迟，以上为估算（`_estimate_tokens = chars/1.6`，价格表为可覆盖默认值）；网络恢复后应以真实 usage 为准，网络往返延迟离线不可测。Embedding 本地化省去外部调用成本，检索段纯 CPU 无外部费用。

---

## 9. 端到端评测（scripts/evaluate_end_to_end.py，Mock 离线 + `--llm live` 已实跑）

用确定性 Mock LLM 离线跑通**完整 `chat()` 链路**（检索→提示构建→LLM→结构化解析→引用渲染），度量管线正确性与延迟；`--llm live` 调真实模型（离线不可达即 exit(1)，不假装跑通）。**mock 数字度量"管线正确性 + 延迟"，非模型答案质量**——答案内容由假 LLM 按提示中的史料生成，质量指标须 live 才有意义。**`--llm live` 真实模型 198 条已实跑**（路由/引用渲染/延迟，见 §14.5 对照一；顺带修复生产 `llm_max_tokens` 空 content bug）。

**44 条全量实测（similarity 模式，mock，2026-09-01 第二轮时点）**：

| 指标 | 结果 |
|------|------|
| 单相关路由正确率 | 32/32（100%，含辛弃疾→苏轼，见 §2.3） |
| 库外拒绝（path=none 且回答无引用） | 6/6 |
| 路由综合正确率 | 38/38（100%，单相关 + 库外） |
| 越界引用被丢弃数 | 0（--bad-cite 故意越界 [999] 6/6 全被 chat() 校验丢弃 → grounding 通过） |
| 带引用（【参考史料】）回答占比 | 38/38 |
| no-RAG 分支确认（库外回答均不带引用） | 6/6 |
| 端到端延迟 | 检索 p50=39.0ms / LLM（mock 本地解析）≈0 / 合计 p50=39.3ms |
| 期望实体出现在回答 | 28/58（0.483；mock 只引用首条史料片段，属宽松管线指标，随真实史源 chunk 变长而分母统计口径变化） |

**引用 grounding 验证**：`--bad-cite` 让 mock 故意声明越界索引 [999]，`chat()` 的 cited_sources 校验全部丢弃且不渲染 footer——端到端证明"回答引用必然指向本次检索集合"，与生成层 Citation 评测的落点一致。

**口径说明**：路径判定为 `_retrieve_knowledge` 实际返回（库外经决策门 2 返回 None），回答无引用 + path=none 双条件判拒绝，避免 mock 噪音。

---

## 10. 多格式文档解析

`src/retrievers/document_loader.py` 把 txt / md / html / pdf / docx / xml / json / csv / tsv / xlsx 统一解析为 `Document`，元数据（title/source/character/url/category/file/aliases）与既有 txt 语义一致，支持前置头【来源】【URL】【人物】【分类】与 `---` 正文分隔、按文件名推断人物、别名补全。PDF 无文本层（扫描件）明确跳过（不支持 OCR）；未知扩展名跳过。

`load_knowledge_files` 已覆盖全部支持扩展名——*.txt 逐字节等价（178 文件零回归），其余按扩展名分发。`scripts/ingest_documents.py` 支持 `--mode append`（按 file 元数据去重，幂等，重复执行不翻倍）与 `--mode rebuild`（全量重建）。样例语料 `data/documents_sample/`（md / html / json / csv，库外人物"张衡"，前置头与别名完整）演示新知识接入流程。

**解析覆盖实测**：9 种格式（txt / md / html / pdf / docx / xml / json / csv / tsv / xlsx）全部解析成功，人物 / 正文 / 来源 / 别名正确提取；PDF 无文本层按预期跳过。`tests/test_document_loader.py` 11 用例（tmp 手工生成各格式夹具，含最小合法 PDF）全过；`ingest_documents.py --src data/documents_sample --mode rebuild` 成功入库。

---

## 11. 结论

**改进后总览（第一轮）**：单相关 hit@3 由 93.8% 提升至 100%；库外拒绝由 0/6 提升至 **6/6**（自传注入 4/6 → 0/6）；RAGAS Context Recall@3 0.812 → 0.870（当前 similarity）；same/cross/event 受控子集与全部单元测试**零回归**。

**第二轮（检索再增强）总览**：召回层混合检索（`retrieval_mode=hybrid`，稠密×BM25 RRF）在多相关枚举召回上实打实提升（Recall@3 0.178 → 0.211，赤壁之战 0.40→0.60），并在诸葛亮→曹操例上把 hit@1/MRR 反超 similarity（30/32 vs 29/32、0.969 vs 0.948，详见 §7 案例分析）；单相关 hit@3 / 库外拒绝 / 自传注入**零回归**（默认仍 `similarity` 保低延迟与既有口径，见 §7/§8）；分阶段延迟埋点 + 成本估算落地（similarity 全链路 p50 39.5ms，hybrid 101.7ms；每查询 LLM 估算 ¥0.0015，Embedding 本地 ¥0，均为离线估算）；Mock LLM 端到端全链路可离线评测（路由 38/38、库外拒绝 6/6、引用 grounding 丢弃验证通过）；多格式文档解析覆盖 10 种格式且 178 文件零回归。单元测试 40 → **96/96**。

**已修复**：
1. 自传陷阱诸葛亮→曹操（决策门 1 强相关全局门，不再依赖阈值单点）；
2. 库外提问注入自传（决策门 2 库外实体拒绝，三重合取）；
3. 检索无重排、纯 top-k（混合重排：词法 × 稠密 RRF）；
4. 分块策略与别名元数据（短文档整篇、别名入库）。

**剩余边界（如实披露，2026-09 更新）**：
1. ~~辛弃疾→苏轼（固有歧义）~~ **已修复**：决策门 1b（named_other）入库后该例退回全局，苏轼《宋史·苏轼传》真实史源入库使全局 top-1 为苏轼（§2.3）。当前相似度模式仅剩 3 例 hit@1 失分（陆游/曹操/陶渊明，均在 top-3，见 §7 案例分析）；
2. ~~**多相关枚举题** Recall@3 仅 0.178（hybrid 0.211）——单人物过滤架构的设计边界，需多人物联合检索/重排才能支持~~ **已在第三轮落地**：多人物联合检索（`$in` 联合池 + `MULTI_TOP_K=7`），Recall@7 0.745（§14.2）；
3. ~~**生成层质量未实测**——无网络环境仅完成脚手架~~ **已在第三轮实跑**：DeepSeek-v4-flash 198 条全量，Faithfulness 0.227 / Answer Relevancy 0.570 / Citation Accuracy 0.740（§14.5）。

**未实施（作为后续方向）**：cross-encoder 精排（`rerank_mode=cross_encoder` 已留接口，本地缓存 bge-reranker 后自动启用）；人物类型先验的跨人退回倾向。

---

## 12. 复现与口径

| 项 | 值 |
|----|----|
| 评测集 | `data/eval/retrieval_eval_full.json`（**198 条**：单相关 145 / 多相关 31 / 库外负面 22；train 159 / holdout 39，`--split train\|holdout\|all`） |
| 检索参数 | k=`RAG_TOP_K`=3 · 多人物联合池 top=`MULTI_TOP_K`=7 · `rerank_mode=hybrid` · `rerank_k_fetch=15` · `FILTERED_SCORE_RATIO=1.25`（train 子集标定） · `STRONG_GLOBAL_THRESHOLD=0.70` · `GAP_THRESHOLD=0.10`（决策门 2 已改纯人名判据，`weak_match_threshold` 已删） |
| 生成参数 | `LLM_MAX_TOKENS`=1024（deepseek-v4-flash reasoning 模型防空 content）· `temperature=0.8` / `temperature_factual=0.3` |
| 混合检索参数 | `retrieval_mode=similarity|hybrid` · `hybrid_bm25_k=30` · `hybrid_rrf_k=60`（RRF 常数，与重排器同口径） |
| 价格表（估算默认） | `LLM_PRICE_IN=1.0` / `LLM_PRICE_OUT=2.0`（¥/1M token，env 覆盖；估算未实调，真实花费以 DeepSeek 账单为准） |
| 改进前复现 | 环境变量关闭新特性：`RERANK_MODE=none STRONG_GLOBAL_THRESHOLD=999 GAP_THRESHOLD=999`（weak_match 配置已删，门 2 基线为"显式名单 + 全词性专名"旧版） |
| 向量库 | **1357 分块双轨库**（99 persona + 1258 historical：gushiwen 965 + 百度百科 288 + ctext 5），已按新分块/别名元数据重建（`python scripts/build_vector_db.py`，幂等）；稠密检索为**精确平方 L2 确定性计算**（numpy，跨进程可复现，见 §13.2） |
| 检索评测 | `python scripts/evaluate_retrieval_full.py [--split train\|holdout\|all]`（默认 similarity）与 `RETRIEVAL_MODE=hybrid python scripts/evaluate_retrieval_full.py`（hybrid）对比 → §7/§14 |
| 阈值扫描 | `python scripts/evaluate_retrieval_full.py --sweep --split train`（train 标定，holdout 只上报泛化） |
| 延迟/成本 | `python scripts/benchmark_retrieval.py --limit 20`（分阶段 p50/p90/mean + 一次性成本 + LLM 估算）→ §8 |
| 端到端评测 | `python scripts/evaluate_end_to_end.py`（mock 离线全链路）；`--bad-cite` 验证引用丢弃；`--llm live` 已实跑 198 条（§14.5 对照一） |
| 在线生成评测 | `python scripts/evaluate_generation.py --cache /tmp/gen_cache.json`（真实 LLM，198 条全量；`--split` 分层；增量缓存可续跑）→ §14.5 |
| 多格式摄取 | `python scripts/ingest_documents.py --src data/documents_sample --mode rebuild`（append 幂等）→ §10 |
| 回归 | `python -m pytest tests/ -q`（**110/110**，零回归） |
| 数据来源说明 | 双轨：真实史源（gushiwen 古籍原文为主 + ctext/百度百科 21 篇/维基备，doc_type=historical，**97/97 人全覆盖**）+ 内置生成摘要（persona），详见 [DATA_SOURCES.md](DATA_SOURCES.md) |

---

## 13. 数据源升级（真实史源 + Persona 双轨）

本次升级把知识库从「99 篇全部内置生成摘要」升级为**双轨数据源**：真实史源（古诗文网
gushiwen.cn 古籍原文为主源，ctext.org / 中文维基百科为备）+ 内置摘要（降级为
Persona，仅风格参考，不作事实依据），并为每个知识块标注 **朝代·书·篇/卷**，与既有
引用溯源机制配合。网络实测（2026-09）：ctext.org 反爬 403 封锁、中文维基不可达，
**古诗文网可达** → 古籍原文源切换为 gushiwen（`--sources gushiwen` 全量抓取已入库，
ctext 保留为 `--sources ctext` 显式回退）。

### 13.1 schema 变化（无检索语义回归）

| 项 | 变化 |
|----|------|
| 元数据键 | 原 7 键 → 11 键，新增 `doc_type`（persona\|historical）、`dynasty`（朝代）、`book`（出处，去书名号）、`chapter`（篇卷） |
| doc_type 判定 | 前置头显式 `【doc_type】` → 文件名含 `_内置` → 来源=内置知识库 → persona；否则 historical |
| 朝代补齐 | front-matter 缺失时从人物配置（`data/characters/*.yaml`）自动补齐 |
| 数据源映射 | `data/sources/character_sources.json`：97 人 → 朝代 + 1-3 条典籍篇/卷（ctext URL）+ 维基条目名（聚焦 97 人不抓全量《二十四史》） |
| 抓取器 | `scripts/crawl_knowledge.py` 扩展 **GushiwenFetcher**（默认源）+ CtextFetcher + WikipediaFetcher（复用 Session/UA/延迟/`crawl_cache.json` 去重）；`--dry-run` 离线打印计划；百度百科改 `--include-baidu` 可选 |
| gushiwen 解析 | `gushiwen_books.json` 缓存 12 部正史章节目录（史记…清史稿共 2599 章）；`gushiwen_resolution.json` 76 人物→章节 URL 解析表（全书名优先 + 合传 OVERRIDE，如 关羽/张飞/赵云→《关张马黄赵传》、周瑜→《周瑜鲁肃吕蒙传》），全部人工核验 |

### 13.2 双轨检索、白话导读与决策门

- **路径决策不变**：filtered/global 决策门仍基于稠密 top-1 距离，双轨分区发生在决策
  后的 top-k 上。gushiwen 原文入库后（决策正确率一度 0.969→0.812），新增两道修正，
  最终 **决策正确率 1.000、库外拒绝 6/6、自传注入 0/6**：
  - **FILTERED_SCORE_RATIO 重标定 1.20 → 1.25**：gushiwen 原文的跨人高相似陷阱
    （3 例 ratio 恰落在 1.20 之下）用新数据阈值扫描重标；
  - **决策门 1b（named_other，点名他人门）**：查询点名了知识库内且非提问人本人的
    其他人时（如"曹操的军事谋略"问者=诸葛亮）直接退回全局——真实史源入库后
    跨人高相似加剧（诸葛亮传·用兵 vs"曹操军事谋略"距离比 1.2x 漏过旧门），
    需专名证据拉回全局；自我指涉题（含"你"/本人名）不受影响；
- **白话导读锚（gloss）**：gushiwen 抓取时从 persona 取前 2 句现代语简介写入
  front-matter `【白话】`，嵌入时前置 `白话导读：{gloss}`——修"大运河 vs 通济渠"
  类古今用词代差（清史稿·漕运 原文字面含"运河"曾压过隋炀帝本纪的"通济渠"）。
  标注"白话导读"与原文区分，非事实依据；
- **分区**：真实史源按原序在前，persona 补位（有史源引史源、无史源才退摘要）；
- **引用标注**：史料块头 `出处: 隋·《隋书·炀帝纪》 - 古诗文网`；footer
  `【参考史料】[1]《隋书·炀帝纪》- 隋·古诗文网`；persona 标注
  「（内置摘要·非权威史源，仅风格参考）」；
- **严格模式** `PERSONA_FALLBACK=off`：检索 filter 全侧注入 `{"doc_type": "historical"}`
  （稠密 + BM25 + hybrid + MMR），persona 完全移出史实检索。多条件 filter 经
  `merge_filters` 合并为 Chroma 合法 `$and` 表达式（修复了
  `{"character", "doc_type"}` 双键 where 抛 "exactly one operator" 的缺陷）；
- **稠密检索确定性修复**：Chroma 1.5.8 默认 Rust HNSW 近似索引跨进程重建图
  （随机种子），得分在决策边界附近进程间抖动（"大运河"题 隋炀帝 0.877 vs
  林则徐 0.90），导致测试/回答不稳定。已把稠密检索改为**精确平方 L2 全量计算**
  （numpy，~1000 条×512 维毫秒级，与 Chroma/hnswlib 同口径，阈值无需重标），
  测试与回答跨进程完全确定；
- **回归测试**：96/96 全过（新增 loader schema、映射表完整性、抓取器 mock、
  双轨标注/严格模式、crawl CLI `--sources all`→gushiwen+wiki 回归；
  `test_sui_wendi_canal_question`（"开凿大运河的事"短句变体）在确定性修复前是
  Chroma 近似抖动的唯一不稳定用例，修复后稳定通过）。

### 13.3 数据源状态（2026-09-01 时点 —— 已被第三轮阶段1 整改，见 §14.1）

- **在库（178 文件 = 986 分块）**：99 篇 persona（97 人物 + 2 事件，内置摘要）+
  **79 篇真实史源** = **74 篇 gushiwen 古籍原文**（覆盖 76 人解析表中的实际抓取，
  含 唐太宗 旧唐书·太宗本纪 与 魏徵传 同书多章的文件名消歧）+ **5 篇 ctext
  手写真实节选样例**（孔子/秦始皇/诸葛亮/唐太宗/李白，正文为著名可验证段落）；
- **gushiwen 全量抓取已执行**（2026-09 网络实测：ctext 403、中文维基不可达、
  gushiwen 可达 → 源切换后 `--sources gushiwen` 跑通全量）：`gushiwen_books.json`
  缓存 12 部正史 2599 章，`gushiwen_resolution.json` 76 人物→章节 URL 解析表
  （已核验），抓取去重缓存 `crawl_cache.json` 位于 `data/sources/`（避免被当
  JSON 知识文档摄入）；
- **28 人仍为 persona 兜底**（2026-09 实际对账：69 人有史源文件、28 人无）：
  **22 人**映射表有书但 gushiwen 解析表未覆盖/抓取缺失（晋书/新唐书/宋史/明史/
  清史稿部分列传及 史记/汉书/三国志 个别传记，如 炎帝/尧/舜/周武王/霍去病/张飞/
  赵云/司马懿/王羲之/陶渊明/杜甫/白居易/王维/颜真卿/李商隐/杜牧/陆游/文天祥/郑和/
  徐霞客/纪晓岚/郑板桥）+ **6 人**无正史列传、按设计走维基（关汉卿/曹雪芹/孙中山/
  鲁迅/蔡元培/梁启超，维基暂不可达），均属设计内 fallback，网络恢复后可补；
- 重建向量库后抽查：986 分块 = 99 persona + 887 historical，**0 条 historical
  缺失 朝代/书/篇**（详见 [DATA_SOURCES.md](DATA_SOURCES.md)）。

---

## 14. 五大问题整改（第三轮 · 2026-09-02，资料补全 / 多相关召回 / 决策门 / 测试扩容 / 在线验证）

> 本轮按《五大问题整改计划》逐项落地：① 28/97 人资料兜底 → 97/97 全覆盖；
> ② 多相关枚举召回 0.178 → 0.745（top-7 口径，train/holdout 泛化）；③ 决策门在
> 扩容集上重标定 + 简化；④ 评测集 44 → 198（带 train/holdout 分层）；⑤ 在线
> 验证首次实跑（DeepSeek-v4-flash，Faithfulness/Answer Relevancy/Citation
> Accuracy）。**除 §14.5 在线指标为真实 LLM 调用（联网，DeepSeek key）外，其余
> 全部为确定性离线实测，可一键复现。**

### 14.1 问题一：人物资料不齐（28/97 persona 兜底 → 97/97 真实史源）

**整改前**（§13.3 记录）：69 人有史源文件、28 人无——其中 22 人映射表有书但
gushiwen 章节未覆盖，6 人无正史列传按设计走维基（维基不可达）。

**整改后**（已落地，数据为磁盘实测）：

| 类别 | 人数 | 处理 |
|------|------|------|
| A 类（原文已在库） | 7 | 炎帝/尧/舜/周武王/霍去病/张飞/赵云，原文藏在 黄帝/周文王/卫青/关羽 的 gushiwen 文件内；清 `crawl_cache.json` 中对应 URL 后重抓，独立成 `biography_*_gushiwen_*.txt` |
| B 类（正史章节未映射） | 15 | 杜甫/白居易/王维/颜真卿/杜牧/李商隐/陆游/文天祥/郑和/徐霞客/纪晓岚/郑板桥/司马懿/王羲之/陶渊明 → 百度百科（`doc_type=historical`） |
| C 类（无正史列传，按设计走维基） | 6 | 关汉卿/曹雪芹/孙中山/鲁迅/蔡元培/梁启超 → 百度百科兜底（维基暂不可达） |

**现状**（loader 口径实测）：97/97 人均有 `historical` 文件；`historical` 107 篇
（原有 79 gushiwen/ctext + 21 百度百科 + 7 A 类重抓）+ persona 99 篇（97 人 + 2
事件，降级为纯风格参考）。`tests/test_knowledge.py` 新增 97/97 覆盖率断言。
百度百科为**三手资料**（如实标注，DATA_SOURCES.md 已补充 21 篇来源说明）。

### 14.2 问题二：多相关枚举召回低（0.178 → 0.745，top-7 口径）

**根因**（§4 已诊断）：filtered 池只含被问者自传、global 池 top-3 装不下 3-7 个
期望人物；named_other 门只会"无条件退回 global"，无 `$in` 多人物联合检索。

**整改**（`src/agents/history_agent.py` `_detect_multi` / `_retrieve_multi` +
`settings.multi_top_k=7`）：
- **点名式（Case 1）**：查询点名 ≥2 位库内他人（曹操、刘备、孙权…）→
  `{"character": {"$in": 名单}}` 联合池（hybrid 下稠密+BM25 双通道同过滤），
  BM25 增加多人物掩码（非名单人物置 -inf）；
- **枚举式（Case 2）**：枚举词（有哪些/哪些是…）+ 分类名词（诗人/名将/皇帝…）
  且非自指 → 全局池多样性检索 + 按人物去重（覆盖"唐朝有哪些著名的诗人"这类
  点名不出现的题）；
- 候选池放大（`fetch_k = max(k*3, rerank_k_fetch)`，仅作取池下限）后**按人物去重**
  ——每人保留检索序最优 1 条直到凑满 `multi_top_k`（7）；multi 分支**不做标准重排**
  （人物多样性优先，重排会打乱去重后的人物覆盖，见 `_retrieve_multi` docstring），
  并跳过 ratio/strong_global/named_other 单人物门。

**结果**（`evaluate_retrieval_full.py`，k 口径随检索分支升级为 top-7）：

| 指标（多相关子集） | 改进前 | 改进后 |
|--------------------|--------|--------|
| Recall（口径 top-3 → top-7） | 0.307（@3） | **0.745（@7）** |
| 其中 train（25 条） | 0.291（@3） | 0.732（@7） |
| 其中 holdout（6 条） | 0.372（@3） | **0.803（@7）** |
| 至少命中 1 个期望人物 | 0.839 | **1.000** |
| RAGAS Context Recall@7 / Precision@7 | 0.291 / 0.280 | 0.745 / 0.359 |

> **口径说明**：改进前后 k 从 3 放大到 7（`multi_top_k`，覆盖期望 3-7 人），
> 增益 = 「多人物 `$in` 联合池」+「池容量放大」共同贡献，非单一因素。
> **泛化证据**：holdout（0.803）≥ train（0.732），阈值只在 train 标定、holdout
> 上报，排除同集过拟合（对照组 base：train 0.291 / holdout 0.372 口径同为 @3）。

### 14.3 问题三：决策门堆叠过拟合（44 条同集调参 → 198 集重标定 + 简化）

**整改**：
- **门 2 简化**（`out_of_kb_refusal`）：删去 weak_match 距离双条件，改为纯
  人名判定——`无自称 ∧ 未点名库内人物 ∧ 点名库外实体（显式名单 + jieba
  nr-only 词性，排除朝代/时代名词）` → 走 no-RAG 分支；
- **门 1b named_other 收敛**：仅对"点名 1 位他人"（cross_trap 口径）退回
  global；≥2 人改走 14.2 的 multi 联合池；
- **阈值重标定**：`--sweep --split train` 在 159 条 train 子集扫描，ratio=1.25
  仍为最优，保持 `FILTERED_SCORE_RATIO` 默认值（train 标定、holdout 验证）；
- **接线清理**：`settings.rag_top_k` 接入 `_retrieve_knowledge` 默认 k（原先
  定义但全仓库无引用）。

**结果**（198 集全量 / train / holdout）：

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| 库外拒绝率（negative 22 条） | 12/22 | **22/22** |
| 自传注入率（top1 是被问人物自传） | 3/22 | **0/22** |
| 单相关 hit@1 / hit@3 | 0.897 / 0.986 | 0.897 / 0.986（零回归） |
| 单相关 MRR / 决策正确率 | 0.933 / 0.986 | 0.933 / 0.986 |
| 单相关 holdout 决策正确率 | — | **1.000（29/29）** |

### 14.4 问题四：测试集太小（44 → 198，带 train/holdout）

**整改**：`data/eval/retrieval_eval_full.json` 44 → **198 条**（单相关 145 /
多相关 31 / 库外负面 22），按 type 分层 80/20 随机切 **train 159 / holdout 39**；
`scripts/generate_eval_expansion.py` 以 DeepSeek 辅助扩写 + 人工校验标签
（asker ∈ 97、单相关 expected 有知识文件、negative 实体确在库外）；4 个评测
脚本统一加 `--split train|holdout|all`。

**train/holdout 的意义**：阈值重扫只在 train 上做（§14.3），泛化指标只在
holdout 上报——直接回应"同集调参、泛化没证据"。

### 14.5 问题五：在线验证首次实跑（Faithfulness / Answer Relevancy / Citation Accuracy）

> ✅ 全量 198 条已跑通（PERSONA_FALLBACK=on，带增量缓存可复跑）。PERSONA_FALLBACK=off
> 对照与 evaluate_end_to_end --llm live 结果见本节末"对照"。

**整改**（`scripts/evaluate_generation.py` 加固）：复用生产 `_shared_openai_client`
（timeout=60 + 连接池）；统一 `max_tokens=1024`（deepseek-v4-flash 的
reasoning_content 吃 token 预算，过小则正文截断为 finish_reason=length）；
单条结果增量落盘缓存（网络中断可续跑）；监控 LLM JSON 解析失败率（区分"真
0 分"与"解析失败假 0 分"）。

**冒烟结果**（8 条）：faithfulness 0.174 / answer_relevancy 0.580 /
citation_accuracy 0.472 / 引用覆盖率 0.750 / JSON 解析失败率 11.6%（<20% 阈值）。
其中李白《将进酒》faithfulness=0.000 已核实为**真实信号**：回答自称"史料记载
有限"后补推测，知识库确实缺该诗创作背景原文——低忠实度反映语料缺口而非
评测故障。

**全量结果（2026-09-02 实测，198 条 × DeepSeek-v4-flash，PERSONA_FALLBACK=on）**：

| 指标 | 全量（198） | train（159） | holdout（39） |
|------|-------------|--------------|---------------|
| Faithfulness 忠实度 | **0.227** | 0.229 | 0.220 |
| Answer Relevancy 回答相关性 | **0.570** | 0.570 | 0.568 |
| Citation Accuracy 引用正确性 | **0.740**（n=153） | 0.726（n=125） | 0.804（n=28） |
| 引用覆盖率（回答带引用占比） | 0.773 | 0.786 | 0.718 |
| LLM JSON 解析失败率 | 12.7%（216/1701） | — | — |

- **泛化**：train ≈ holdout（faithfulness 0.229 vs 0.220、relevancy 0.570 vs
  0.568），citation 在 holdout 反而更高（0.726 → 0.804）——在线指标同样无
  同集过拟合迹象；
- **口径说明**：answer_relevancy 由回答反向生成问题做 embedding 余弦（切题
  即≈1，0.57 属中等偏上）；faithfulness 为"被检索上下文支持的声明占比"，
  全局 0.227 受两因素压低——① 无史料支撑的 no-RAG 常识回答记 0 分（22 条库外
  负面 + 检索空窗），② 部分枚举题检索只命中 1-2 人、回答其余期望人物时缺乏
  对应史料支持。**低忠实度是语料覆盖的信号，非评测假象**（§冒烟已核实一例）；
- **成本**：全量 198 条共约 1900 次 LLM 调用（198 生成 + 1701 结构化判定），
  增量缓存落盘可续跑/复用，实际花费按 DeepSeek 计费账单核对。

**对照一：evaluate_end_to_end --llm live（真实模型端到端，2026-09-02）**

第一版实跑 **127/198 条**后中断：`_call_api_with_retry` 对 deepseek-v4-flash 某次返回
空 content 重试耗尽抛异常——与 evaluate_generation.py 同根因（reasoning_content
吃掉 token 预算，生产 `_call_api_with_retry` 未传 max_tokens）。**已修复**：新增
`settings.llm_max_tokens`（默认 1024，`LLM_MAX_TOKENS` 可覆盖）接入生产调用，pytest
110/110 零回归后**重跑完整 198 条**。第一版 127 条统计（修复前）：

| 项 | 结果 |
|----|------|
| 路由分布 | filtered 57 / global 58 / multi 6（库外负面 22 条未跑到，故无 no-RAG） |
| 引用行为 | 127/127 **空引用**——live 回答未按 `[n]` 结构化格式带引用，生产引用解析返回空（真实信号：DeepSeek 未遵守引用格式指令；引用 grounding 逻辑本身已由 Mock 离线验证 38/38） |
| 延迟（真实 LLM，含网络） | mean ≈2.3s / p50 ≈2.1s / p90 ≈4.7s（最大 37.9s） |

**重跑完整 198 条结果（修复 max_tokens 后，全部跑通，exit 0）**：

| 项 | 结果 |
|----|------|
| 路由分布 | filtered 71 / global 74 / multi 31 / **no-RAG 22**（库外负面 22/22 全部正确拒绝，多人物题 31 条走 multi 联合池） |
| 结构化引用索引（`[n]` cite） | **0/198**——live 回答未按 `[n]` 格式带索引（真实信号：DeepSeek 未遵守结构化引用指令） |
| 【参考史料】footer 渲染 | **166/198**：RAG 回答 176 条中 166 条带引用（94.3%）；**no-RAG 22 条 0 条带引用**（库外拒绝不注入引用 ✓）；越界引用丢弃 0 |
| 期望实体率（宽松：人物名出现在回答中） | 198/255 = **0.776** |
| 延迟（真实 LLM，含网络，198 条） | mean ≈4.1s / p50 ≈3.3s / p90 ≈6.4s / max 22.8s |

> 注：第一版日志"空引用"实为结构化 cite 索引为空；footer 渲染的【参考史料】引用
> 是存在的（166/198）——修正第一版措辞。e2e live 的意义在于**真实模型下路由、
> 引用渲染与延迟的端到端验证**：路由分配合规（库外 22/22 拒绝、多人物 31 条走
> multi），引用渲染 94.3%、库外不注入引用，延迟 p90 6.4s 可接受。

**对照二：PERSONA_FALLBACK=off（严格模式，persona 完全退出史实检索）**

同 93 条（前段单相关为主）对比（增量缓存可续跑完整 198 条）：

| 指标（同 93 条） | PERSONA_FALLBACK=on（主跑） | =off（严格） | 方向 |
|------|------|------|------|
| Faithfulness | 0.252 | **0.260** | ↑ 史源严格化后回答更忠实 |
| Answer Relevancy | 0.553 | **0.569** | ↑ |
| Citation Accuracy | **0.720**（n=76） | 0.591（n=73） | ↓ 可引用的史源块变少 |
| 引用覆盖率 | **0.817** | 0.785 | ↓ |

**解读**：移除 persona 后回答转向纯史源、更忠实（faithfulness/relevancy 微升），
但部分问题原本靠 persona 补位提供可引用文本，严格模式下引用来源不足导致
citation/覆盖率下降——这印证"persona 是风格补位、非事实依据"的双轨设计，也
暴露严格模式下史源覆盖仍有缺口（如 A 类 7 人 gushiwen 节选较短、百度百科 21 篇
正文切块）。**完整 198 条可续跑**：`PERSONA_FALLBACK=off python scripts/evaluate_generation.py --cache /tmp/gen_cache_nopersona.json`。

---

### 14.5 更新行（2026-09-02 深夜）

> - **e2e live**：首版 127/198 中断 → **修复 `llm_max_tokens`（新增 `LLM_MAX_TOKENS` env 接线生产 `_call_api_with_retry`，pytest 110/110 零回归）后重跑完整 198 条**，路由/引用/延迟见上表。
> - **PERSONA_FALLBACK=off 对照**：93/198 中途快照（见上表），结论明确；完整可续跑。

### 14.6 结论与回归

- 五大问题全部落地：资料 97/97、多相关 Recall@7 0.745（holdout 0.803）、
  库外拒绝 22/22、评测集 198（train/holdout）、在线三指标真实跑通；
- **零回归**：单相关 hit@1 0.897 / MRR 0.933 / 决策 0.986 与改进前一致；
  110 条单元测试全过（+13：BM25 多人物掩码 2、aliases/multi 检测 10、
  knowledge 覆盖率 1）；
- **局限与诚实声明**：百度百科为三手资料（权威性低于正史原文）；多相关
  top-3 → top-7 口径变化使增益无法拆分为"联合池"与"容量"各自的贡献；在线
  指标为 198 条 × 多轮 LLM 调用（约 1900 次：198 生成 + 1701 结构化判定，实际
  花费按 DeepSeek 计费账单核对；PERSONA_FALLBACK 开/关两档对照见更新行）；库外
  实体名单为有限集，未收录的库外人物可能漏检。
