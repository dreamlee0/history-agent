# Core (版本与构建向量库的本地环境严格一致，避免 chromadb 0.x/1.x 格式不兼容)
langchain==1.2.15
langchain-core==1.5.3
langchain-community==0.4.1
langchain-text-splitters==1.1.1
langchain-chroma==1.1.0
langchain-openai==1.1.12

# OpenAI兼容 SDK (LLM用，可接智谱/DeepSeek/OpenAI等)
openai==2.38.0

# 本地 Embedding (HuggingFace, 免费无需 API Key)
langchain-huggingface==1.2.2
sentence-transformers==5.4.1
transformers==4.51.1

# numpy/scipy 钉死（2026-09-03 修复 Streamlit Cloud 装依赖失败）：
# streamlit==1.32.0 硬性要求 numpy<2；而 sentence-transformers 拉入的 scipy 无上限
# （scipy>=1.0.0），pip 会挑最新版 scipy 1.18.1（要求 numpy>=2.0.0），两者直接冲突，
# 云端报「installer returned a non-zero exit code」。钉回本地实测可用的组合
# （numpy 1.26.2 + scipy 1.15.1，本机 110 测试全过）保持 Cloud 与本地解析一致；
# 勿升 scipy>=1.16（那批要求 numpy 2，会与 streamlit 1.32 的 numpy<2 重新冲突）。
numpy==1.26.2
scipy==1.15.1

# 向量数据库 (必须 1.x，与已提交的向量库格式匹配，切勿降到 0.x)
chromadb==1.5.8

# Document Loaders
pypdf>=3.0.0
python-docx>=0.8.11
beautifulsoup4>=4.12.0
lxml>=4.9.0
requests>=2.31.0
curl_cffi>=0.16.0  # 百度百科抓取：Chrome TLS 指纹绕过 WAF 403（可选，缺省时退回 requests）

# Web UI
# 钉死 1.32.0：按钮居中/样式注入经本地 playwright 在 1.32 全视口验证（0.0px 偏差），
# 新版 Streamlit 会改按钮外层 HTML/CSS（可能包进 flex 容器导致 margin:auto 失效），不可控版本漂移
streamlit==1.32.0

# PDF导出
reportlab>=4.0.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.10.1
pyyaml>=6.0
