# history-rag-chat 测评与优化总报告

> **对象**：history-rag-chat（LangChain + Chroma + bge-small-zh-v1.5 + DeepSeek-v4-flash）
> **测评时间**：2026-09-03（检索层确定性离线实测） / 2026-09-04（生成层真实 DeepSeek-v4-flash 重测）
> **评测集**：198 条扩展集（train 159 / holdout 39，按类型分层 80/20）；生成层为 198 的**50 条代表性分层抽样**（在线成本约束）
> **数据规模**：97 人物 / 21 朝代 / 206 文件（107 真实史源 + 99 persona）/ 1357 向量块
> **结论一句话**：检索层确定性可复现、多相关与库外拒绝大幅提升且 holdout 验证无过拟合；生成层经判定层噪声修复后 Faithfulness 0.227 → **0.636**（非负类 **0.723**），结构化引用 18/20（90%）可靠渲染；全部单元测试 114/114 零回归。

---

## 一、系统组成

| 层 | 组件 | 说明 |
|----|------|------|
| 数据层 | 双轨知识库 | 真实史源（gushiwen 古籍原文 74 篇 + 百度百科 21 篇 + ctext 样例 5 篇 + A 类重抓 7 篇）`doc_type=historical`；内置生成摘要 99 篇 `doc_type=persona`（仅风格参考，非事实依据） |
| 检索层 | Chroma + bge-small-zh-v1.5 | 稠密精确平方 L2 确定性计算（跨进程可复现）；可选 BM25 词法 × 稠密 RRF 混合检索 |
| 重排 | `rerank_mode=hybrid` 默认 | jieba + TF-IDF 词法 × 稠密向量 RRF 融合（完全离线，K=60） |
| 决策门 | 防污染门 + 库外拒绝门 | `FILTERED_SCORE_RATIO=1.25`（train 标定）+ 决策门 1 强相关全局门 + 门 1b named_other + 门 2 纯人名判据 |
| 生成层 | DeepSeek-v4-flash（OpenAI 兼容） | `response_format=json_object` 强制结构化引用 + 端点降级 + 解析失败定向重试；`max_tokens=1024` 防空 content |
| 对话记忆 | SQLite + 内存（RLock 加锁） | session_id + 人物名复合键隔离，多会话/多角色不串记 |
| 前端 | Streamlit（水墨丹青） | 开始界面 / 人物选择 / 结构化溯源 / Markdown·PDF 导出 |

---

## 二、测评口径

### 2.1 评测集（`data/eval/retrieval_eval_full.json`）

| 类别 | 条数 | 说明 |
|------|------|------|
| 单相关（same/cross/event/cross_trap） | 145 | 基础受控集 + 高相似跨人陷阱（诸葛亮问曹操、辛弃疾问苏轼等） |
| 多相关（multi） | 31 | 主题枚举题（"赤壁之战涉及哪些人物"等），期望人物为一组 3–7 人 |
| 库外负面（negative） | 22 | 查询对象不在 97 人知识库（钱学森、袁隆平、拿破仑等） |
| 分层 | train 159 / holdout 39 | 阈值只在 train 标定，泛化指标只在 holdout 上报 |

### 2.2 指标定义

- **单相关**：Hit@1 / Hit@3（=Recall@3）/ MRR / 防污染门决策正确率，k=3；
- **多相关**：Recall@3 / Recall@7（命中期望人物数 ÷ 期望人物数，第三轮口径升级为 @7）；RAGAS 风格 Context Recall / Precision；
- **库外负面**：拒绝率（走 no-RAG 分支）× 自传注入率（top-1 为被问人物自传）；
- **生成层**：Faithfulness（回答被检索上下文支持的声明占比）/ Answer Relevancy / Citation Accuracy / 引用覆盖率 / LLM JSON 解析失败率；
- **结构化引用**：真实模型下带【参考史料】footer 的回答占比（`chat()` 仅在解析成功且有合法引用时追加 footer）。

### 2.3 复现命令（一键可复现）

```bash
# 检索层评测（确定性离线）
python scripts/evaluate_retrieval_full.py --split holdout     # 泛化指标（默认 similarity）
RETRIEVAL_MODE=hybrid python scripts/evaluate_retrieval_full.py  # 混合检索对比
python scripts/evaluate_retrieval_full.py --sweep --split train  # 阈值标定

# 端到端（Mock LLM 离线）
python scripts/evaluate_end_to_end.py

# 生成层（真实 LLM，需 .env DeepSeek key，增量缓存可续跑）
python scripts/evaluate_generation.py --cache /tmp/gen_cache.json
# 生成层 50 条代表性抽样（当前报告口径，预检 198 → 固定种子抽 50 → 串行）
~/anaconda3/bin/python /tmp/gen_sample.py   # 缓存 ~/.gen_sample.json，可续跑

# 回归
python -m pytest tests/ -q          # 114/114
```

---

## 三、测评结果（当前定稿）

### 3.1 检索层（确定性离线实测）

| 指标 | 结果 | 说明 |
|------|------|------|
| 单相关 hit@1 / hit@3 | **0.897 / 0.986** | 145 条，198 集全量口径 |
| 单相关 MRR / 决策正确率 | **0.933 / 0.986** | 决策门防跨人污染 |
| 单相关 holdout 决策正确率 | **1.000（29/29）** | 泛化无过拟合 |
| 多相关 Recall@7 | **0.745**（train 0.732 / holdout **0.803**） | 基线 @3=0.307 |
| 多相关「至少命中 1 期望人物」 | **1.000** | 改进前 0.839 |
| RAGAS Context Recall@7 / Precision@7 | **0.745 / 0.359** | 多相关子集 |
| 库外拒绝率 | **22/22** | 改进前 12/22 |
| 自传注入率 | **0/22** | 改进前 3/22 |

> **泛化证据**：holdout ≥ train（多相关 0.803 vs 0.732、决策正确率 1.000）——阈值仅在 train 标定、holdout 上报，排除同集过拟合。

### 3.2 端到端（Mock LLM 离线，44 条全链路）

| 指标 | 结果 |
|------|------|
| 路由综合正确率（单相关 + 库外） | **38/38（100%）** |
| 库外拒绝（path=none 且回答无引用） | 6/6 |
| 越界引用被丢弃（`--bad-cite` 故意越界） | 0（6/6 全被 `chat()` 校验丢弃） |
| 带引用（【参考史料】）回答占比 | 38/38 |
| 端到端延迟（mock） | p50 ≈39ms |

### 3.3 生成层（真实 DeepSeek-v4-flash，50 条代表性抽样）

> **口径**：198 全量因在线成本约束改为代表性分层抽样 50 条（固定种子 42：same 14 / event 4 / cross 12 / cross_trap 6 / multi 8 / negative 6），串行重测、判定层 3 轮多数投票 + 否定性声明规则（修复过程见 CHANGELOG 2026-09-04 条目）。

| 指标 | 抽样全量（50） | 非负类（44） | train（39） | holdout（11） |
|------|----------------|--------------|-------------|---------------|
| Faithfulness 忠实度 | **0.636** | **0.723** | 0.678 | 0.488 |
| Answer Relevancy 回答相关性 | **0.604** | — | 0.608 | 0.591 |
| Citation Accuracy 引用正确性 | **0.735**（n=44） | — | 0.705（n=35） | 0.852（n=9） |
| 引用覆盖率（回答带引用占比） | 0.880 | — | — | — |
| LLM JSON 解析失败率 | **0.0%**（0/999） | — | — | — |

> **结论**：0.227 → **0.636**（非负类 0.723），提升 3 倍——旧 0.227 主因是判定层单次判定噪声（20.6% 声明重复判定翻转，非检索/覆盖故障），3 轮多数投票修复；残余低分确为语料覆盖缺口（商鞅变法、鲁迅新文化运动、唐太宗贞观之治、玄奘经书数目等），非评测故障。

### 3.4 结构化引用（真实模型 e2e live，2026-09-03 修复后抽样 20 条）

| 指标 | 结果 |
|------|------|
| 带【参考史料】引用回答占比 | **18/20（90%）** |
| 越界引用丢弃 | 0 |
| 路由正确 | 20/20 |
| 期望实体率 | 15/20（0.750） |

> 修复前 0/198 为双重假象：① 生产调用未传 `response_format=json_object`，模型不输出 JSON；② e2e 对 live 模式 `cited` 硬编码 `[]`——有一半是测出来的假零。修复后结构化引用在真实模型下可靠渲染，`[n]` 编号与 footer 由代码侧生成并校验，可溯源保证成立。**20 条为抽样验证，勿与 198 条并列**；全量可 `python scripts/evaluate_end_to_end.py --llm live`（付费，约 15 分钟）。

### 3.5 工程与性能

| 指标 | 结果 |
|------|------|
| 单元测试 | **114/114 通过**（含 97/97 覆盖率断言、json_object 接线、BM25 多人物掩码等） |
| 检索延迟（similarity / hybrid） | 全链路 p50 **39.5ms / 101.7ms** |
| 一次性成本 | 986 篇 embedding 本地推理 ~16.1s（**¥0**）；BM25 索引 ~3.6s |
| LLM 成本估算 | 每查询 ≈ ¥0.0015（输入 p50 1333 token / 输出 ~125 token，默认价格表，离线估算非实调） |
| e2e live 延迟（真实 LLM 含网络） | mean ≈4.1s / p50 ≈3.3s / p90 ≈6.4s / max 22.8s |

---

## 四、优化结果（改进前后对比）

### 4.1 关键指标总览

| 指标 | 改进前 | 改进后 | 提升来源 |
|------|--------|--------|----------|
| 多相关枚举召回 | 0.307（@3） | **0.745（@7）** | P2 多人物联合检索 + 池放大 |
| 多相关「至少命中 1 人」 | 0.839 | **1.000** | 同上 |
| 库外拒绝率 | 12/22（首轮 0/6） | **22/22（首轮 6/6）** | P3 门 2 纯人名判据 |
| 自传注入率 | 3/22（首轮 4/6） | **0/22（首轮 0/6）** | 决策门 1/1b/2 组合 |
| 单相关 hit@3 | 93.8%（首轮） | **100%（首轮）/ 0.986（198 集零回归）** | 精确检索 + 陷阱题修复 |
| 人物资料覆盖 | 69/97 有史源，28 人 persona 兜底 | **97/97 真实史源全覆盖** | P1 资料补全 |
| 检索确定性 | Chroma HNSW 近似索引进程间抖动 | **精确平方 L2，跨进程可复现** | DS2 |
| 结构化引用（真实模型） | 0/198 | **18/20（90%）** | 2026-09-03 json_object 修复 |
| 单元测试 | 40 → 96/96 | **114/114** | 测试扩容 + 回归 |

### 4.2 五大问题整改（2026-09-02 第三轮）

| # | 问题 | 整改内容 | 结果 |
|---|------|----------|------|
| P1 | 人物资料不齐（28/97 persona 兜底） | A 类 7 人清 URL 重抓独立成文件；B 类 15 人 + C 类 6 人（无正史列传、维基不可达）→ 百度百科（`doc_type=historical`） | **97/97 人均有 historical 文件**（107 historical + 99 persona） |
| P2 | 多相关枚举召回低 | 点名式 `{"character": {"$in": 名单}}` 联合池 + BM25 多人物掩码；枚举式全局池多样性 + 按人物去重；池放大至 `MULTI_TOP_K=7` | Recall@7 **0.745**（holdout 0.803 ≥ train 0.732） |
| P3 | 决策门堆叠过拟合 | 门 2 删距离双条件改**纯人名判据**；门 1b named_other 收敛到点名 1 人；阈值 train 子集重扫仍 1.25 最优 | 库外拒绝 **22/22**、自传注入 **0/22**、单相关零回归 |
| P4 | 测试集太小（44 → 198） | 扩到 198 条（单相关 145 / 多相关 31 / 库外 22），train/holdout 分层；标签人工校验 | 泛化指标可上报、阈值只在 train 标定 |
| P5 | 在线验证首次实跑 | 复用生产 client、`max_tokens=1024` 防空 content、增量缓存、JSON 解析失败率监控；判定层 3 轮多数投票修复噪声 | 初测 0.227 系**判定噪声**（20.6% 声明翻转）；修复后 50 条抽样 Faithfulness **0.636** / Relevancy 0.604 / Citation 0.735 |

### 4.3 数据源升级与检索确定性（2026-09）

| # | 项 | 内容 | 效果 |
|---|----|------|------|
| DS1 | 真实史源入库 + persona 双轨 | gushiwen 古籍原文为主源（ctext 403 / 维基不可达实测后切换）；front-matter 朝代·书·篇卷 → 元数据；史源优先、persona 补位；白话导读锚 | 178 文件 = 986 分块，0 条缺朝代/书/篇 |
| DS2 | 检索确定性修复 | Chroma Rust HNSW 近似索引 → **精确平方 L2 全量计算** | 决策边界得分跨进程稳定，flaky 用例 3/3 通过 |
| DS3 | 阈值重标定 | `FILTERED_SCORE_RATIO` 1.20 → **1.25**（gushiwen 入库后跨人高相似陷阱加剧） | 44 条集决策 1.000、库外拒绝 6/6 |
| DS4 | 审查轮修复 | 爬虫 `--sources all` 展开 bug、`--use-db` 导入 bug、XML 解析器、文档口径 | 96/96 全过 |

### 4.4 结构化引用修复（2026-09-03）

- **根因 1（可靠性）**：生产调用未传 `response_format=json_object` → 真实模型常不输出 `{reply, cited_sources}`；
- **根因 2（测量盲区）**：e2e 对 live 模式 `cited` 硬编码 `[]` → 0/198 有一半是测出来的假零；
- **修复**：`response_format={"type":"json_object"}` 从 API 层强制合法 JSON；端点 400/422 不支持时自动降级普通输出（`_json_mode` 实例开关，不崩溃）；解析失败做一次「只输出 JSON」强约束定向重试；e2e 改为以回复中【参考史料】footer 判定成功；
- **提示词强化**：新增规则 4「检索到史料必须至少引用一条」——把 73%（强化前 11/15）拉到 **90%**；
- **最终**：真实模型带引用 **18/20（90%）**、越界丢弃 0、路由 20/20。余量 10% 为模型偶发输出空引用列表，**经确认保持诚实引用，不加代码侧自动补引**（自动补引会指向模型未实际依据的史料，与 Citation Accuracy 指标有张力）。

### 4.5 工程健壮性修复（H / M / L 摘要）

| 类别 | 关键修复 |
|------|----------|
| H1–H7 | 命名修正（RAG 对话机器人非 Agent）；Streamlit 会话 ID 随机 UUID 防多用户串数据；记忆加锁 + SQLite 恢复；XSRF 恢复默认开启；建库幂等（连跑不翻倍）；LLM 空响应兜底；重试只针对瞬时错误 |
| M1–M10 | 温度分档（有 RAG 用 0.3 贴近史实）；记忆最近 N 条可配；MMR 检索接口；「删除对话」联删 DB；溯源展示 URL；冷启动说明；PDF 字体提示 |
| L1–L12 | README/朝代/技术栈口径统一；删除冗余爬虫；死代码清理；CI 工作流；`get_logger()`；角色判定改 `isinstance`；session_state 收敛；XSS 转义；run.sh 无 .env 不阻断 |

---

## 五、遗留边界（如实披露）

1. **相似度模式仅剩 3 例 hit@1 失分**（陆游/曹操/陶渊明，均命中 top-3）——近义人物语义抢占的固有难点；hybrid 可修复曹操例（hit@1 30/32 vs 29/32）；
2. **多相关口径 k 从 3 升级到 7**（`multi_top_k`）——增益为「联合池 + 池放大」共同贡献，非单一因素；
3. **百度百科为三手资料**（非正史原文），如实标注、仅作引用溯源；中文维基暂未入库（网络不可达）；
4. **混合检索默认不开启**（默认 `similarity`）：hybrid 在多相关与 MRR 上有收益，但延迟更高（p50 101.7ms vs 39.5ms），且既有测试/评测口径以 similarity 为准；
5. **严格模式（PERSONA_FALLBACK=off）**：回答更忠实但引用覆盖率下降，暴露史源覆盖仍有的缺口（该对照为修复前的旧判定口径，仅作方向性参考）；
6. **未实施**：cross-encoder 精排（接口已留，本地缓存 bge-reranker 后自动启用）、流式输出、人物类型先验的跨人退回倾向。
7. **生成层为 50 条代表性抽样而非全量 198**（在线成本约束，用户决策）：holdout 仅 11 条，Faithfulness 0.488 低于 train（0.678）主要来自 2 条 negative 设计零 + 已知覆盖缺口，n 小不具统计意义；全量复跑需付费（约 50 条量级的 4 倍），缓存机制支持续跑。

---

## 六、验收对照（Definition of Done）

| 验收项 | 结果 |
|--------|------|
| 单相关零回归（198 集） | ✅ hit@1 0.897 / MRR 0.933 / 决策 0.986 |
| 多相关目标（≥0.6） | ✅ Recall@7 0.745（holdout 0.803） |
| 库外拒绝 | ✅ 22/22；自传注入 0/22 |
| 在线评测跑通（真实 LLM） | ✅ 50 条代表性抽样三指标真实数字入报告；判定噪声修复后 Faithfulness 0.636（非负类 0.723） |
| 结构化引用（真实模型） | ✅ 18/20（90%），越界丢弃 0 |
| 全量测试 | ✅ 114/114 零回归 |
| 确定性 | ✅ 检索层离线可复现（精确 L2）；生成层付费可复跑（增量缓存） |

---


